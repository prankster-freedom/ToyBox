indexes:
  - kind: ChatMessage
    properties:
      - name: user_uid
      - name: timestamp
        direction: desc

  - kind: PersonalityAnalysis
    properties:
      - name: user_uid
      - name: timestamp
        direction: desc
