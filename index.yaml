indexes:
  - kind: ChatMessage
    properties:
      - name: user_uid
      - name: timestamp
        direction: desc

  - kind: ChatMessage
    properties:
      - name: user_uid
      - name: timestamp
        direction: asc

  - kind: PersonalityAnalysis
    properties:
      - name: user_uid
      - name: timestamp
        direction: desc
